# TODO - 31 de Diciembre 2025

## 🎯 TAREA PRIORITARIA: Migrar session-manager a whatsapp-web.js

### Problema Actual
- **venom-bot** tiene bug crítico: loop infinito con tokens viejos, nunca genera QR nuevo
- Se queda en ciclo: "Was disconnected!" → "Disconnected by cell phone!" → reintenta infinitamente
- Bug en `folderNameToken`: ignora ruta configurada y usa `/src/tokens/` en vez de `/tokens/`

### Solución: Migrar a whatsapp-web.js

**Razones:**
1. ✅ Más estable y confiable (usado exitosamente en whatsapp-massive-sender)
2. ✅ **PERSISTENCIA DE SESIÓN**: LocalAuth guarda en disco → reconecta automáticamente después de reinicios
3. ✅ **NO PIDE QR DE NUEVO**: Una vez vinculado, persiste para siempre
4. ✅ Comunidad más grande y activa
5. ✅ Documentación superior
6. ✅ No tiene el bug de folderNameToken de venom

**Desventajas aceptables:**
- Usa más memoria (no crítico en servidor Contabo)
- Arranque un poco más lento (aceptable para servicio que corre 24/7)

---

## 🔑 OBJETIVO CRÍTICO: Sesión Persistente

### Requisito del Cliente
**"Una vez que el cliente vincule el QR, ya nunca se desconecte aunque reiniciemos el servidor"**

### Cómo lo logra whatsapp-web.js

**LocalAuth Strategy:**
```
Primera vez:
1. Usuario escanea QR
2. WhatsApp Web autentica
3. LocalAuth guarda credenciales en: /tokens/client_51/
   - Default/IndexedDB/
   - Default/Local Storage/
   - session.json (metadatos)
4. ✅ Conectado

Reinicios posteriores (pm2 restart):
1. sessionService.js ejecuta client.initialize()
2. LocalAuth detecta carpeta /tokens/client_51/ existe
3. Lee credenciales guardadas
4. ✅ Reconecta automáticamente SIN pedir QR
5. emit('ready') → cliente listo

Cuando SÍ pide QR de nuevo:
- Si borras manualmente /tokens/client_51/
- Si WhatsApp invalida sesión (cambio de teléfono, desvinculaste en app)
- Si archivos de sesión están corruptos
```

### Ventaja vs venom-bot
- **venom:** Tokens se corrompen fácilmente → loop infinito (problema actual)
- **whatsapp-web.js:** LocalAuth más robusto → reconecta confiablemente

### Pasos de Migración

#### 1. Instalar whatsapp-web.js
```bash
cd /root/leadmaster-central-hub
npm install whatsapp-web.js@latest
```

#### 2. Actualizar sessionService.js
**Archivo:** `/root/leadmaster-central-hub/src/modules/session-manager/services/sessionService.js`

**Cambios:**
- Reemplazar `venom-bot` por `whatsapp-web.js`
- Usar `LocalAuth` para gestión de sesiones
- Mantener misma API externa (clientSessions Map)
- Callbacks: `qr`, `ready`, `authenticated`, `auth_failure`, `disconnected`

**Ejemplo de configuración:**
```javascript
const { Client, LocalAuth } = require('whatsapp-web.js');

const client = new Client({
    authStrategy: new LocalAuth({
        clientId: name,
        dataPath: path.join(__dirname, '../../../tokens')
    }),
    puppeteer: {
        headless: true,
        executablePath: '/usr/bin/google-chrome-stable',
        args: [
            '--no-sandbox',
            '--disable-setuid-sandbox',
            '--disable-dev-shm-usage',
            '--disable-accelerated-2d-canvas',
            '--no-first-run',
            '--no-zygote',
            '--disable-gpu'
        ]
    }
});

client.on('qr', (qr) => {
    // Generar QR en base64
    const sess = clientSessions.get(clienteId);
    if (sess) {
        sess.qr = qr; // qr es el código string
        // Convertir a base64 con librería qrcode
    }
});

client.on('ready', () => {
    // Cliente listo
    const sess = clientSessions.get(clienteId);
    if (sess) {
        sess.ready = true;
        sess.connecting = false;
    }
});

client.on('authenticated', () => {
    console.log('✅ Autenticado');
});

client.on('auth_failure', (msg) => {
    console.error('❌ Error autenticación:', msg);
});

client.on('disconnected', (reason) => {
    console.log('⚠️ Desconectado:', reason);
    // Limpiar sesión
});

await client.initialize();
```

#### 3. Actualizar métodos de envío
**Cambio de API:**
- venom: `client.sendText(number, message)`
- whatsapp-web.js: `client.sendMessage(chatId, message)`

**Formato de número:**
- venom: `54911XXXXXXXX`
- whatsapp-web.js: `54911XXXXXXXX@c.us`

#### 4. Probar QR Generation
1. Limpiar todos los tokens viejos
2. Iniciar leadmaster-hub
3. Login como Haby
4. Conectar WhatsApp → debe mostrar QR
5. Escanear con tu móvil (Alberto)
6. Verificar estabilidad

#### 5. Testing
- [ ] QR se genera correctamente
- [ ] QR se muestra en frontend
- [ ] Escaneo exitoso conecta sesión
- [ ] **✅ CRÍTICO: Sesión persiste después de `pm2 restart leadmaster-hub`**
- [ ] **✅ CRÍTICO: NO pide QR de nuevo al reiniciar (usa tokens guardados)**
- [ ] **✅ CRÍTICO: Reinicio del servidor NO desconecta WhatsApp**
- [ ] Envío de mensaje funciona después de reinicio
- [ ] Listener recibe mensajes después de reinicio
- [ ] Verificar `/tokens/client_51/` contiene archivos de sesión
- [ ] Probar 3 reinicios consecutivos → debe reconectar automáticamente cada vez

#### 6. Manejo de Reconexión Automática
**Implementar en sessionService.js:**

```javascript
client.on('disconnected', async (reason) => {
    console.log('⚠️ WhatsApp desconectado:', reason);
    const sess = clientSessions.get(clienteId);
    
    if (sess) {
        sess.ready = false;
        sess.connecting = true;
    }
    
    // IMPORTANTE: NO eliminar tokens, dejar que LocalAuth reconecte
    console.log('🔄 Intentando reconectar automáticamente...');
    
    // Esperar 5 segundos y reintentar
    setTimeout(async () => {
        try {
            await client.initialize();
            console.log('✅ Reconexión iniciada');
        } catch (error) {
            console.error('❌ Error en reconexión:', error);
        }
    }, 5000);
});

client.on('auth_failure', (msg) => {
    console.error('❌ Fallo de autenticación:', msg);
    const sess = clientSessions.get(clienteId);
    
    if (sess) {
        sess.ready = false;
        sess.connecting = false;
        // SOLO aquí limpiamos tokens si falla autenticación
        sess.qr = 'REQUERIDO'; // Señal para mostrar QR de nuevo
    }
});
```

**Flujo de Reconexión:**
1. Servidor reinicia → `client.initialize()`
2. LocalAuth encuentra tokens → reconecta sin QR
3. Si falla conexión → espera 5s → reintenta
4. Si falla autenticación → limpia tokens → pide QR nuevo

#### 7. Documentación
Actualizar archivos:
- `/root/leadmaster-central-hub/docs/ARQUITECTURA_UNIFICACION.md`
- Agregar nota sobre migración de venom a whatsapp-web.js
- Razones técnicas del cambio
- **Garantía de persistencia de sesión**

### Archivos a Modificar

1. **package.json**
   - Ya tiene whatsapp-web.js? Verificar versión
   - Si no, agregar: `"whatsapp-web.js": "^1.25.0"` (o latest)

2. **src/modules/session-manager/services/sessionService.js**
   - Líneas 1-20: Imports
   - Líneas 70-140: Configuración de venom → reemplazar completamente

3. **src/modules/session-manager/controllers/sessionController.js**
   - Verificar si necesita ajustes en endpoint QR
   - Posiblemente necesite generar base64 desde string QR

### Tiempo Estimado
- Implementación: 1-2 horas
- Testing: 30 minutos
- Documentación: 15 minutos
- **TOTAL: ~2.5 horas**

### Backup Antes de Empezar
```bash
cd /root/leadmaster-central-hub
git add .
git commit -m "Checkpoint antes de migrar de venom a whatsapp-web.js"
git push
```

### Notas Adicionales
- Mantener venom-bot instalado temporalmente (por si necesitamos rollback)
- Una vez validado funcionamiento, remover venom de dependencies
- El bot-responder puede seguir usando venom (está funcionando bien allí)

---

## 📌 Contexto del Problema de Hoy

**Estado actual (30 Dic 2025):**
- leadmaster-central-hub con venom-bot: NO FUNCIONA (loop infinito)
- whatsapp-massive-sender con whatsapp-web.js: FUNCIONA
- whatsapp-bot-responder con venom-bot: FUNCIONA (pero depende de massive-sender)

**Decisión:** Migrar leadmaster a whatsapp-web.js para tener sistema estable y autónomo.

**Usuario:** Alberto (puede testear con su móvil)
**Cliente final:** Haby (conectará cuando todo esté estable)

---

## 📋 CHECKLIST FINAL: Validación de Persistencia

### Escenario 1: Primera Conexión
```bash
# Limpiar tokens viejos
rm -rf /root/leadmaster-central-hub/tokens/client_51*

# Iniciar servicio
pm2 restart leadmaster-hub

# En frontend (como Haby):
# 1. Login → WhatsApp → Ver QR
# 2. Escanear con móvil de Alberto
# 3. ✅ Debe conectar y mostrar "Conectado"
# 4. Verificar: ls -la /root/leadmaster-central-hub/tokens/client_51/
#    Debe tener: Default/, .wwebjs_auth/, session.json
```

### Escenario 2: Reinicio del Servidor (CRÍTICO)
```bash
# Con sesión activa (después de Escenario 1)
pm2 restart leadmaster-hub

# Esperar 10-15 segundos
pm2 logs leadmaster-hub --lines 30

# Buscar en logs:
# ✅ "Inicializando WhatsApp para cliente 51"
# ✅ "Cliente 51 WhatsApp listo" (SIN pedir QR)
# ❌ NO debe aparecer "qr" o "catchQR"

# En frontend:
# Estado debe cambiar a "Conectado" automáticamente
# NO debe pedir escanear QR de nuevo
```

### Escenario 3: Múltiples Reinicios
```bash
# Probar 5 reinicios consecutivos
for i in {1..5}; do
  echo "Reinicio $i/5"
  pm2 restart leadmaster-hub
  sleep 20
  # Verificar logs - debe reconectar cada vez
done

# ✅ ÉXITO: Todas las 5 veces reconecta sin QR
# ❌ FALLA: Si pide QR en algún reinicio → bug en LocalAuth
```

### Escenario 4: Envío Después de Reinicio
```bash
# Después de Escenario 2 o 3
# En frontend: Campañas → Crear campaña test → Enviar 1 mensaje

# ✅ Mensaje debe enviarse exitosamente
# ✅ WhatsApp debe permanecer conectado
```

### Escenario 5: Desconexión Manual (simular caída)
```bash
# En el móvil: WhatsApp → Dispositivos Vinculados → Cerrar sesión Web

# El servidor debe:
# 1. Detectar desconexión (event 'disconnected')
# 2. Logs: "WhatsApp desconectado"
# 3. Logs: "Intentando reconectar automáticamente"
# 4. ❌ Falla reconexión (tokens invalidados)
# 5. Frontend debe mostrar "Desconectado - Escanea QR"

# Usuario escanea QR de nuevo → debe volver a conectar
```

### ✅ Criterios de Aceptación

**DEBE CUMPLIR:**
1. Primera vez: muestra QR, escanea, conecta ✅
2. Reinicio servidor: reconecta SIN QR ✅
3. 5 reinicios: reconecta automáticamente cada vez ✅
4. Envíos funcionan después de reinicio ✅
5. Tokens persisten en `/tokens/client_51/` ✅
6. Logs NO muestran "qr" después de primer escaneo ✅

**OPCIONAL (mejorado):**
7. Auto-reconexión después de desconexión temporal ✅
8. Notificación en frontend cuando reconecta ✅

---

## 🚨 Troubleshooting

### Problema: Sigue pidiendo QR después de reinicio
**Causa:** LocalAuth no encuentra tokens
**Solución:**
```bash
# Verificar que dataPath es correcto
ls -la /root/leadmaster-central-hub/tokens/client_51/

# Verificar permisos
chmod -R 755 /root/leadmaster-central-hub/tokens/
```

### Problema: Loop infinito de reconexión
**Causa:** Tokens corruptos
**Solución:**
```bash
# Limpiar tokens y empezar de nuevo
rm -rf /root/leadmaster-central-hub/tokens/client_51/
pm2 restart leadmaster-hub
# Escanear QR de nuevo
```

### Problema: "Error: Session closed"
**Causa:** WhatsApp detectó uso inusual o múltiples sesiones
**Solución:**
- Esperar 24 horas antes de reconectar
- Usar número de teléfono diferente para testing
- Verificar que no hay otra sesión Web activa en el mismo número
